query {
  feed: feed {
    url
    description
  }

  filtered_feed: feed(filter:"QL") {
    id
    description
    url
    postedBy {
      id
      name
    }
  }

  feed_ids: feed {
    id
  }

  first_link: link(id: "1") {
    url
    description
  }
}