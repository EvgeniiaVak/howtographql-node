query {
  feed: feed {
    links{
      url
    },
    count
  }

  filtered_feed: feed(filter:"QL") {
    links{
      description
    }
  }

  first_link: link(id: "1") {
    url
    description
  }
}